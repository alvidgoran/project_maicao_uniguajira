<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso de internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/descarga"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_maicao_uniguajira"
        tools:targetApi="31">

        <activity
            android:name=".proyecto.menuActivity"
            android:exported="false" />
        <activity
            android:name=".proyecto.notificationsActivity"
            android:exported="false" />
        <activity
            android:name=".proyecto.searchActivity"
            android:exported="false" />
        <activity
            android:name=".proyecto.logUp2Activity"
            android:exported="false" />
        <activity
            android:name=".proyecto.logUpActivity"
            android:exported="false" />
        <activity
            android:name=".proyecto.dashboardActivity"
            android:exported="false" />
        <activity
            android:name=".proyecto.loginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
